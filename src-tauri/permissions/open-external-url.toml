"$schema" = "../gen/schemas/schema.json"

[[permission]]
identifier = "open-external-url"
description = "Allow Slack Native to ask the app to open validated external web URLs in the system browser."
commands.allow = ["open_external_url"]
